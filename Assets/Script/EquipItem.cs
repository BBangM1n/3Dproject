using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EquipItem : Item
{
    public enum EquipType { Armor, Helmet, Hammer, Gun } //enum : 열거형 타입
    public EquipType equiptype;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
